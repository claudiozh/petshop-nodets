# Petshop nodets
Projeto feito em node usando typesscript

## Pré-requisitos globais:
`npm i -g nodemon typescript ts-node`

## Instalação:
`npm install`

## Start projeto:
`npm run start:dev`

## Deploy heroku
`heroku login`

`heroku create`

`git push heroku`
